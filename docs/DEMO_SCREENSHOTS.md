# Demo Screenshots

This document records the intended demo presentation state for the Gradio app.

Use screenshots that show:

- the professional header
- a supported claim with evidence and citation validity
- a refuted claim with backend metadata visible
- a not-enough-info claim with fallback state visible
- the architecture / limitations tabs

Recommended capture order:

1. Desktop full view with the hero section and input form
2. Supported verdict with evidence table
3. Refuted verdict with metadata panel
4. Not-enough-info verdict with fallback indicators
5. Research mode tab or configuration panel

Keep screenshots current with the checked-in UI so the README and demo remain aligned.
