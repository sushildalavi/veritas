# Demo Screenshots

This document records the intended screenshot state for the React claim-verification workspace.

Use screenshots that show:

- the professional header and backend status
- the empty input state with the validation snapshot visible
- a live verdict with confidence, citation validity, and latency
- evidence cards rendered from the live API

Recommended capture order:

1. Desktop full view with the hero section and input form
2. Empty workspace with the current service metadata visible
3. Live verification result with evidence and explanation

Keep screenshots current with the checked-in UI so the README and product docs remain aligned.
