# Offline optional extension only.
base_model: microsoft/phi-2
peft:
  r: 8
  alpha: 16
  dropout: 0.05
training:
  max_seq_length: 1024
  batch_size: 1
  gradient_accumulation_steps: 8
  output_dir: checkpoints/qlora_phi
